# HTML & CSS Basics — Assignment #1

Student: Zhhass — Group SE-2526

## What's here

- `index.html` / `task1.css` — Task 1: personal page with profile card
- `task2.html` / `task2.css` — Task 2: div + float layout (header, sidebar, content, footer)
- `task3.html` / `task3.css` — Task 3: tribute page for Anthony Edwards
- `task4.html` / `task4.css` — Task 4: grades table + feedback form
- `common.css` — shared navbar styles used on every page

All pages share a navbar so you can move between tasks.

## How to publish on GitHub Pages

1. Create a new repository on GitHub.
2. Upload all files in this folder to the repository root (keep `index.html` at the root).
3. Go to **Settings → Pages**, choose the `main` branch and `/root` folder, then save.
4. Your site will be live at `https://your-username.github.io/your-repo-name`.

## Notes

- Profile and tribute page images are placeholders (generated avatars) — swap in real photos before submitting if required.
- Task 2 uses only `<div>` elements with `float`/positioning, no Flexbox or Grid, per the assignment rules.
